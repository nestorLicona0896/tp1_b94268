para compilar el programa ejecute la instruccion: make

para ejecutar el codigo use la instruccion: ./bin/envios o bin/envios 


para correr pruebas unitarias, ejecutar:

make test

y luego ejecute:

./bin/tests o bin/tests